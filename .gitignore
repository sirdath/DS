# Dependencies
node_modules/
.pnpm-store/

# Builds / caches
dist/
build/
.next/
.turbo/
out/
coverage/
.cache/
*.tsbuildinfo

# Env + secrets
.env
.env.*
!.env.example
# Catch any stray notes-with-secrets at any depth
**/*secret*.txt
**/*password*.txt
**/passwords*
**/secrets*

# OS / editor
.DS_Store
Thumbs.db
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
.idea/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Client-specific + ephemeral assets (CDN'd, not committed)
assets/motion-sequences/generated/
apps/*/public/research/*/scrape-cache/

# Blender experimental work (heavy binaries, not deliverable)
assets/BLENDER3DANIMATION/
**/cache_fluid*/
*.blend1

# Image source files (exported versions live in public/)
assets/icons-website/
/*-UPSCALE-PHOTO.png
/*-UPSCALED-PHOTO.png

# Claude Code ephemeral
.claude/logs/

# Scratch / working dirs (screenshots, experiments — never deliverable)
.scratch/

# Original DS2 handoff bundle (123 MB of source HTML/screenshots/uploads — kept local)
DS2-CONSULTING/

# Secrets (local only)
.webui_secret_key

# Raw video source recordings (heavy; site ships compressed mp4s in public/hero)
*.mov
/assets/VIDEO-SCROLL-clean/

# Claude Code ephemeral
.claude/tmp/
Icon

# Stel's brand-identity repo, cloned in as its own git repo (Stel777/DS2-BRAND-MATERIALS).
# Lives inside the folder for convenience but is NOT part of the monorepo's git.
/DS2-BRAND-MATERIALS/
