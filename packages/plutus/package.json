{
  "name": "@ds/plutus",
  "version": "0.0.1",
  "private": true,
  "description": "Plutus — DS2 accounts-receivable collections engine. Ingests invoices/customers/payments, computes AR state + a deterministic risk×value chase list, and uses Claude only to draft bilingual (el/en) escalating reminders that a human approves before sending. Pure deterministic core; idempotent; auditable; multi-tenant.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src"],
  "scripts": {
    "demo": "tsx scripts/demo.ts",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.104.1"
  },
  "devDependencies": {
    "@ds/tsconfig": "workspace:*",
    "@types/node": "^22.16.5",
    "tsx": "^4.19.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.0"
  }
}
