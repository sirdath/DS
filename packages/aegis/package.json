{
  "name": "@ds/aegis",
  "version": "0.0.1",
  "private": true,
  "description": "Aegis — DS2's challenge-first site audit. Point it at a URL; it pulls the technical reality (Core Web Vitals, WCAG accessibility, SEO, best practices) via the PageSpeed Insights API and runs Claude over it to produce a prioritised, plain-language scorecard with the EU Accessibility Act exposure spelled out. The lead-magnet that opens the door.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src"],
  "scripts": {
    "demo": "tsx scripts/demo.ts",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.104.1"
  },
  "devDependencies": {
    "@ds/tsconfig": "workspace:*",
    "@types/node": "^22.16.5",
    "tsx": "^4.19.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.0"
  }
}
