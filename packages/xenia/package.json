{
  "name": "@ds/xenia",
  "version": "0.0.1",
  "private": true,
  "description": "Xenia — DS2 AI receptionist. A channel-agnostic conversation + booking engine: it talks to a customer in Greek or English, checks availability, and books an appointment via a Claude tool-use loop. Source-agnostic — messaging (Telegram/WhatsApp) and phone-voice plug in behind a channel adapter seam.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src"],
  "scripts": {
    "demo": "tsx scripts/demo.ts",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.104.1"
  },
  "devDependencies": {
    "@ds/tsconfig": "workspace:*",
    "@types/node": "^22.16.5",
    "tsx": "^4.19.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.0"
  }
}
